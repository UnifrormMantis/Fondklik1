# Настройка CryptoBot для обработки платежей

## 1. Создание аккаунта CryptoBot

1. Перейдите на [CryptoPay](https://crypt.bot/)
2. Нажмите "Get Started" или "Начать"
3. Войдите через Telegram аккаунт
4. Подтвердите номер телефона

## 2. Создание приложения

1. В личном кабинете нажмите "Create App"
2. Заполните форму:
   - **App Name**: Crypto Payment Bot
   - **Description**: Бот для пополнения баланса через USDT
   - **Webhook URL**: оставьте пустым (используем polling)
3. Нажмите "Create"

## 3. Получение API токена

1. В созданном приложении найдите раздел "API"
2. Скопируйте "API Token"
3. Токен выглядит примерно так: `12345:ABCdefGHIjklMNOpqrsTUVwxyz`

## 4. Настройка бота

1. Откройте файл `bot.py`
2. Найдите строку:
   ```python
   CRYPTO_BOT_TOKEN = "YOUR_CRYPTO_BOT_TOKEN_HERE"
   ```
3. Замените на ваш реальный токен:
   ```python
   CRYPTO_BOT_TOKEN = "ваш_токен_от_cryptobot"
   ```

## 5. Пополнение баланса CryptoBot

1. В личном кабинете CryptoBot перейдите в "Balance"
2. Нажмите "Top up" или "Пополнить"
3. Выберите USDT (TRC20)
4. Отправьте USDT на указанный адрес
5. Дождитесь подтверждения (обычно 1-2 минуты)

## 6. Тестирование

1. Запустите бота: `python3 bot.py`
2. Отправьте `/start` боту
3. Попробуйте создать тестовый платеж на 1 USDT
4. Проверьте, что счет создается корректно

## 7. Безопасность

- **Никогда не публикуйте** ваш API токен
- **Не добавляйте** токен в git репозиторий
- **Используйте** переменные окружения для продакшена

## 8. Переменные окружения (рекомендуется)

Создайте файл `.env`:
```
BOT_TOKEN=ваш_telegram_bot_token
CRYPTO_BOT_TOKEN=ваш_cryptobot_token
```

И обновите `bot.py`:
```python
import os
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv('BOT_TOKEN')
CRYPTO_BOT_TOKEN = os.getenv('CRYPTO_BOT_TOKEN')
```

## 9. Комиссии CryptoBot

- **Создание счета**: бесплатно
- **Пополнение**: 0.5% от суммы
- **Вывод средств**: 0.5% от суммы

## 10. Поддерживаемые валюты

- USDT (TRC20)
- USDT (ERC20)
- BTC
- ETH
- LTC
- TON

## Готово! 🎉

Теперь ваш бот может:
- Создавать счета для оплаты
- Отслеживать статус платежей
- Автоматически зачислять средства
- Обрабатывать ошибки платежей

















