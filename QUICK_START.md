# 🚀 Быстрый старт - Постоянная работа бота

## ⚡ Запуск за 30 секунд

```bash
# Запустить главное меню
./manage_bot.sh

# Выбрать вариант 1 для постоянной работы
# Или сразу:
./manage_bot.sh 1
```

## 🎯 Что происходит

1. **Системный сервис** устанавливается в macOS
2. **Бот запускается** автоматически
3. **Работает постоянно** - даже после перезагрузки
4. **Автоматически перезапускается** при сбоях

## ✅ Проверка работы

```bash
# Проверить статус
./manage_bot.sh 6

# Посмотреть логи
./manage_bot.sh 7
```

## 🛑 Остановка

```bash
# Остановить все боты
./manage_bot.sh 5
```

## 🆘 Если что-то пошло не так

```bash
# Остановить все
./manage_bot.sh 5

# Подождать 5 секунд
sleep 5

# Запустить заново
./manage_bot.sh 1
```

## 📋 Все команды

| Команда | Описание |
|---------|----------|
| `./manage_bot.sh 1` | Системный сервис (рекомендуется) |
| `./manage_bot.sh 2` | Демон с автоперезапуском |
| `./manage_bot.sh 3` | Фоновый режим |
| `./manage_bot.sh 4` | Обычный запуск |
| `./manage_bot.sh 5` | Остановить все |
| `./manage_bot.sh 6` | Проверить статус |
| `./manage_bot.sh 7` | Показать логи |

## 🎉 Готово!

После выполнения `./manage_bot.sh 1` ваш бот будет работать постоянно, независимо от состояния терминала Cursor!











